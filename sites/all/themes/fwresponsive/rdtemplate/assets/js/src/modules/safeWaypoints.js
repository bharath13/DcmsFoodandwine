'use strict'

/*
This is an modulated version of the Waypoints library, since a third party code was
overwriting the version which was saved to the window.
 */

require('waypoint');

module.exports = window.Waypoint;